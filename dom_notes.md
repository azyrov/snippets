
# node
- an abstract base upon which objects in the dom are based on
- because it is abstract, an identity can then be conferred onto the node subclass
    - eg. of nodes
        - element node
        - text node
        - comment node
- different nodes have different interfaces
    - eg. of node interfaces
        - node atrributes
        - node characters
        -   