// const usersArray = [
//   { id: "u1", name: "Alice", role: "Admin" },
//   { id: "u2", name: "Bob", role: "User" }
// ];

// // Transforming an array into a key-value lookup map
// const userMap = usersArray.reduce((acc, curr) => {
//   acc[curr.id] = { name: curr.name, role: curr.role };
//   return acc; // You must return the accumulator for the next iteration
// }, {});

// console.log(userMap);

// let arr = [
//     {id: "1", name: "John", age: 20},
//     {id: "2", name: "Kofi", age: 30},
//     {id: "3", name: "Ama", age: 40}
// ]

// // using reduce to getthe id as the object names
// let obj = arr.reduce((acc, curr) => {
//     acc["id"] = { id: curr.id, name: curr.name,age: curr.age
//  };
//     return acc;
// }, {}) 

// console.log(obj)

